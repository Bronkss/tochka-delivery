meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: npm install --include=optional && npm run build:all

run:
  command: npm run start
  containerPort: 3000