meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: npm install --include=optional && npm run build:all

run:
  command: node dist-server/server.js
  containerPort: 3000